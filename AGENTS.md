use bun; don't use npm or node
use global frontend-design skill when modifying UI.
use global tailwind-design-system skill when modifying css.
After making large changes, use your global simplify-code skill to clean up any messy code.
Use your global clean-code skill to refactor any large code files.
Always run a check before running a test
NEVER run the dev server as I run it myself and you'll mess things up if you try to
